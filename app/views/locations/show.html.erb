<p id="notice"><%= notice %></p>

<h2>You are currently at:</h2>

<p>
	<%= @location.my_location %>
</p>

<%= link_to "Edit location", edit_location_path(@location) %>

<h2>The closests buses are...</h2>

<% if @bus_count == 0 %>

	<p>
		...not really that close.  Order an Uber.
	</p>

	<% end -%>

	<% @nearby_buses.each do |bus| %>
		<p>
			<strong>Route:</strong> <%= bus["ROUTE"] %><br>
			<strong>Bus #:</strong> <%= bus["VEHICLE"] %><br>
			<strong>NEXT STOP:</strong> <%= bus["TIMEPOINT"] %><br>
		</p>
	<% end -%>
